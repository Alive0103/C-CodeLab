# C-CodeLab 项目结构说明

## 📁 项目整体结构

```
C-CodeLab/
├── backend/                    # 后端 Spring Boot 项目
│   ├── src/main/java/com/codelab/
│   │   ├── application/        # 应用服务层
│   │   │   ├── AuthService.java           # 认证服务
│   │   │   ├── CodeExecutionService.java # 代码执行服务
│   │   │   ├── CodeSnippetService.java   # 代码片段服务
│   │   │   └── RedisTestService.java     # Redis测试服务
│   │   ├── domain/             # 领域模型层
│   │   │   ├── User.java                  # 用户实体
│   │   │   ├── CodeSnippet.java          # 代码片段实体
│   │   │   ├── ExecutionRecord.java      # 执行记录实体
│   │   │   └── repository/               # 数据访问接口
│   │   │       ├── UserRepository.java
│   │   │       ├── CodeSnippetRepository.java
│   │   │       └── ExecutionRecordRepository.java
│   │   ├── infrastructure/     # 基础设施层
│   │   │   ├── common/                   # 通用组件
│   │   │   │   └── ApiResponseCode.java
│   │   │   ├── config/                  # 配置类
│   │   │   │   ├── AsyncConfig.java      # 异步配置
│   │   │   │   ├── JwtConfig.java        # JWT配置
│   │   │   │   ├── RedisConfig.java      # Redis配置
│   │   │   │   ├── SecurityConfig.java  # 安全配置
│   │   │   │   └── WebSocketConfig.java  # WebSocket配置
│   │   │   └── security/                # 安全组件
│   │   │       ├── JwtAuthenticationFilter.java
│   │   │       ├── JwtTokenUtils.java
│   │   │       ├── PasswordUtils.java
│   │   │       └── RequestLoggingFilter.java
│   │   ├── interfaces/         # 接口层
│   │   │   ├── web/                     # Web接口
│   │   │   │   ├── annotation/          # 自定义注解
│   │   │   │   ├── dto/                 # 数据传输对象
│   │   │   │   ├── AuthController.java
│   │   │   │   ├── CodeController.java
│   │   │   │   ├── ResultController.java
│   │   │   │   ├── UserCodeController.java
│   │   │   │   ├── UserProfileController.java
│   │   │   │   ├── ApiResponse.java
│   │   │   │   └── GlobalExceptionHandler.java
│   │   │   └── ws/                      # WebSocket接口
│   │   │       └── ExecutionWebSocketHandler.java
│   │   ├── service/            # 业务服务层
│   │   │   └── UserService.java
│   │   └── CCodeLabApplication.java     # 主启动类
│   ├── src/main/resources/
│   │   ├── application.yml              # 应用配置
│   │   └── sql/                         # SQL脚本
│   │       ├── schema.sql               # 数据库结构
│   │       └── data.sql                 # 初始数据
│   ├── target/                          # 编译输出目录
│   ├── pom.xml                          # Maven配置
│   ├── start-with-tidb.bat              # TiDB启动脚本
│   └── start-with-tidb.sh               # TiDB启动脚本(Linux)
├── front/                       # 前端 Vue.js 项目
│   ├── src/
│   │   ├── api/                 # API接口层
│   │   │   ├── auth.ts          # 认证API
│   │   │   ├── code.ts          # 代码API
│   │   │   ├── http.ts          # HTTP客户端
│   │   │   └── user.ts          # 用户API
│   │   ├── components/          # Vue组件
│   │   │   ├── EditorPane.vue   # 代码编辑器
│   │   │   ├── HistoryList.vue  # 历史记录列表
│   │   │   └── OutputPane.vue   # 输出面板
│   │   ├── stores/              # Pinia状态管理
│   │   │   └── auth.ts          # 认证状态
│   │   ├── utils/               # 工具函数
│   │   │   └── websocket.ts     # WebSocket工具
│   │   ├── views/               # 页面视图
│   │   │   ├── CodeHistoryView.vue  # 代码历史页面
│   │   │   ├── EditorView.vue        # 编辑器页面
│   │   │   ├── LoginView.vue         # 登录页面
│   │   │   ├── RegisterView.vue      # 注册页面
│   │   │   └── UserProfileView.vue   # 用户资料页面
│   │   ├── styles/              # 样式文件
│   │   │   └── index.css        # 全局样式
│   │   ├── App.vue              # 根组件
│   │   ├── main.ts              # 入口文件
│   │   └── router.ts            # 路由配置
│   ├── node_modules/            # 依赖包目录
│   ├── package.json            # 依赖配置
│   ├── package-lock.json       # 依赖锁定文件
│   ├── tsconfig.json           # TypeScript配置
│   └── vite.config.ts          # Vite配置
├── docs/                       # 文档目录
│   ├── 后端详细设计.md
│   ├── 前端详细设计.md
│   ├── JWT认证系统实现说明.md
│   └── 项目结构说明.md
├── .gitignore                  # Git忽略文件
├── LICENSE                     # 开源许可证
└── README.md                   # 项目说明文档
```

## 🏗️ 架构设计

### 后端架构 (DDD + 分层架构)

```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Web Controllers│  │  WebSocket     │  │   DTOs         │ │
│  │   - Auth        │  │  - Execution   │  │   - Request    │ │
│  │   - Code        │  │  - Result      │  │   - Response   │ │
│  │   - User        │  │                │  │                │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                    Application Layer                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Auth Service  │  │  Code Service   │  │  User Service   │ │
│  │   - Register    │  │  - Execution    │  │  - Profile      │ │
│  │   - Login       │  │  - Save         │  │  - Management   │ │
│  │   - JWT         │  │  - History      │  │                │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                      Domain Layer                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Entities      │  │   Repositories  │  │   Value Objects │ │
│  │   - User        │  │   - UserRepo    │  │   - AuthToken   │ │
│  │   - CodeSnippet │  │   - CodeRepo    │  │   - Execution   │ │
│  │   - Execution   │  │   - ExecRepo    │  │   - Result      │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                 Infrastructure Layer                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Security      │  │   Data Access   │  │   External      │ │
│  │   - JWT         │  │   - JPA/Hibernate│  │   - Redis      │ │
│  │   - Password    │  │   - MySQL/TiDB  │  │   - WebSocket    │ │
│  │   - Filter      │  │   - Repository  │  │   - Async       │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 前端架构 (Vue 3 + Composition API)

```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Views         │  │   Components   │  │   Router        │ │
│  │   - Login       │  │   - Editor     │  │   - Guards      │ │
│  │   - Editor      │  │   - Output     │  │   - Routes      │ │
│  │   - History     │  │   - History    │  │                │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                    Business Layer                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Stores        │  │   Services     │  │   Utils        │ │
│  │   - Auth Store  │  │   - API Client │  │   - WebSocket  │ │
│  │   - User Store  │  │   - HTTP Client│  │   - Validators  │ │
│  │   - Code Store  │  │   - Auth Logic │  │   - Helpers    │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                    Data Layer                                │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   API Layer     │  │   HTTP Client  │  │   WebSocket     │ │
│  │   - Auth API    │  │   - Axios      │  │   - Connection  │ │
│  │   - Code API    │  │   - Interceptors│  │   - Messages    │ │
│  │   - User API    │  │   - Error Handle│  │   - Events      │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 核心组件说明

### 后端核心组件

1. **JWT认证系统**
   - `JwtTokenUtils` - JWT令牌工具类
   - `JwtAuthenticationFilter` - JWT认证过滤器
   - `AuthService` - 认证服务

2. **代码执行系统**
   - `CodeExecutionService` - 代码执行服务
   - `ExecutionWebSocketHandler` - WebSocket处理器
   - 异步执行和结果推送

3. **数据访问层**
   - `UserRepository` - 用户数据访问
   - `CodeSnippetRepository` - 代码片段数据访问
   - `ExecutionRecordRepository` - 执行记录数据访问

### 前端核心组件

1. **认证系统**
   - `auth.ts` - 认证状态管理
   - `LoginView.vue` - 登录页面
   - `RegisterView.vue` - 注册页面

2. **代码编辑器**
   - `EditorPane.vue` - 代码编辑组件
   - `OutputPane.vue` - 输出显示组件
   - `HistoryList.vue` - 历史记录组件

3. **状态管理**
   - `auth.ts` - 认证状态
   - `http.ts` - HTTP客户端配置
   - `websocket.ts` - WebSocket连接管理

## 📊 数据流图

### 用户认证流程
```
用户 → 前端登录 → HTTP请求 → 后端认证 → JWT生成 → Redis存储 → 返回令牌 → 前端存储
```

### 代码执行流程
```
用户输入 → 前端发送 → 后端接收 → 验证认证 → 异步执行 → 结果推送 → 前端显示
```

### 数据持久化流程
```
业务操作 → 服务层 → 仓储层 → JPA/Hibernate → 数据库 → 返回结果
```

## 🚀 部署架构

### 开发环境
```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Frontend  │    │   Backend   │    │   Database  │
│   (Vue.js)  │◄──►│ (Spring Boot)│◄──►│  (MySQL)    │
│   Port:3000 │    │  Port:8081  │    │  Port:3306  │
└─────────────┘    └─────────────┘    └─────────────┘
                           │
                           ▼
                   ┌─────────────┐
                   │    Redis    │
                   │  Port:6379  │
                   └─────────────┘
```

### 生产环境
```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Nginx     │    │   Backend   │    │   Database  │
│  (Static)   │◄──►│ (Spring Boot)│◄──►│  (TiDB)     │
│   Port:80   │    │  Port:8081  │    │  Port:4000  │
└─────────────┘    └─────────────┘    └─────────────┘
                           │
                           ▼
                   ┌─────────────┐
                   │    Redis    │
                   │  Port:6379  │
                   └─────────────┘
```

## 📝 开发规范

### 代码规范
- 后端使用 Java 17 + Spring Boot 3.x
- 前端使用 Vue 3 + TypeScript
- 统一使用 UTF-8 编码
- 遵循 RESTful API 设计规范

### 命名规范
- 类名使用 PascalCase
- 方法名使用 camelCase
- 常量使用 UPPER_SNAKE_CASE
- 数据库表名使用 snake_case

### 提交规范
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式调整
- refactor: 代码重构
- test: 测试相关
- chore: 构建过程或辅助工具的变动

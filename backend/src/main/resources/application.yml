server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://gateway01.ap-northeast-1.prod.aws.tidbcloud.com:4000/c_test?useSSL=true&requireSSL=true&serverTimezone=UTC&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: 2GtHVk27Cjofv3g.root
    password: qBoQciGIrjAeB61a
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000
      validation-timeout: 5000
      leak-detection-threshold: 60000
      connection-test-query: SELECT 1
      initialization-fail-timeout: 0
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    serialization:
      write-dates-as-timestamps: false
  data:
    redis:
      password: Ln216812_
      username: redisuser
      host: redis-18586.c93.us-east-1-3.ec2.cloud.redislabs.com
      port: 18586
      ssl:
        enabled: true

code:
  tempDir: ${java.io.tmpdir}/code-env
  gccPath: gcc # On Windows, ensure gcc is on PATH (e.g., from Mingw64 or WSL)
  sandboxImage: c-codelab-sandbox:latest
  dockerTimeout: 15
  containerPool:
    size: 5 # 容器池大小
    maxIdleTime: 300 # 容器最大空闲时间（秒），超过此时间会被清理并重新创建

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    com.codelab: DEBUG
    com.zaxxer.hikari.HikariConfig: DEBUG
    com.zaxxer.hikari.pool.HikariPool: DEBUG
    com.mysql.cj.jdbc: DEBUG
# 本地运行说明

## 📋 架构说明

- **Java后端服务**：在本地运行（不在Docker中）
- **前端服务**：在本地运行（使用Vite开发服务器）
- **C代码执行**：用户提交的C代码在Docker沙箱容器中运行，确保安全隔离
- **数据库和Redis**：使用原有的云服务配置（不在Docker中）

## 🚀 启动步骤

### 1. 构建沙箱镜像（只需执行一次）

```powershell
docker build -t c-codelab-sandbox:latest ./code-sandbox
```

或者使用docker-compose（仅构建镜像，不启动服务）：

```powershell
docker-compose build
```

### 2. 启动后端服务（本地）

```powershell
cd backend
mvn spring-boot:run
```

或者如果已经构建过：

```powershell
cd backend
java -jar target/c-codelab-0.0.1-SNAPSHOT.jar
```

后端服务将运行在：http://localhost:8081

### 3. 启动前端服务（本地）

```powershell
cd front
npm install
npm run dev
```

前端服务将运行在：http://localhost:5173

## ⚙️ 配置说明

### 后端配置

后端服务需要能够访问Docker daemon来创建和管理沙箱容器。

**Windows用户**：
- 确保Docker Desktop正在运行
- 后端服务会自动通过Docker Desktop的API访问Docker

**Linux/Mac用户**：
- 确保Docker服务正在运行
- 如果后端服务运行在容器中，需要挂载Docker socket
- 如果后端服务在本地运行，直接访问本地Docker即可

### 沙箱配置

在 `application.yml` 中配置：

```yaml
code:
  tempDir: ${java.io.tmpdir}/code-env  # 本地临时目录
  sandboxImage: c-codelab-sandbox:latest
  dockerTimeout: 15
```

## 🔍 验证

1. **检查沙箱镜像是否存在**：
```powershell
docker images | findstr c-codelab-sandbox
```

2. **测试代码执行**：
   - 访问前端：http://localhost:5173
   - 编写C代码并执行
   - 用户提交的C代码将在独立的Docker沙箱容器中编译和执行
   - 执行结果会保存到数据库并返回给用户

## 📝 注意事项

1. **Docker必须运行**：后端服务需要Docker来创建沙箱容器
2. **端口占用**：
   - 后端：8081
   - 前端：5173
3. **数据库和Redis**：使用原有的云服务配置（在application.yml中）

## 🛑 停止服务

- **后端**：在运行后端服务的终端按 `Ctrl+C`
- **前端**：在运行前端服务的终端按 `Ctrl+C`
- **Docker容器**：代码执行后会自动清理，无需手动停止

## 📚 相关文档

- [DOCKER沙箱使用说明.md](./DOCKER沙箱使用说明.md) - 详细的沙箱执行机制说明
- [README.md](./README.md) - 项目总体说明

